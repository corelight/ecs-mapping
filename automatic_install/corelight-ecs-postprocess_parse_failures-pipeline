{
  "description": "A pipeline to handle logs that have critical parse failures that could prevent field collisions and other improper handling of data that could cause issues in mappings/fields/values. This pipeline is from the Github repository https://github.com/corelight/ecs-mapping. Please file all questions or issues with this configuration in the corresponding Github repository.",
  "version": 11221,
  "processors": [
    {
      "set": {
        "field": "event.ingested",
        "value": "{{_ingest.timestamp}}",
        "if": "ctx.event?.ingested == null"
      }
    },
    {
      "set": {
        "field": "@timestamp",
        "value": "{{event.ingested}}",
        "if": "ctx['@timestamp'] == null"
      }
    },
    {
      "set": {
        "field": "error.message",
        "value": "Processor {{ _ingest.on_failure_processor_type }} with tag {{ _ingest.on_failure_processor_tag }} in pipeline {{ _ingest.on_failure_pipeline }} failed with message {{ _ingest.on_failure_message }}"
      }
    },
    {
      "remove": {
        "field": "id",
        "ignore_failure": true,
        "ignore_missing": true
      }
    },
    {
      "pipeline": {
        "description": "Set naming for parse_failures",
        "name": "corelight-ecs-postprocess_parse_failures-index_naming-pipeline",
        "ignore_missing_pipeline": false
      }
    }
  ]
}
