{
  "description": "Corelight PCR pipeline. This pipeline is from the Github repository https://github.com/corelight/ecs-mapping. Please file all questions or issues with this configuration in the corresponding Github repository.",
  "processors": [
    {
      "set": {
        "field": "temporary_metadata_index_name_suffix",
        "value": "various",
        "ignore_failure": false
      }
    },
    {
      "set": {
        "field": "event.category",
        "value": "observations",
        "ignore_failure": true
      }
    },
    {
      "rename": {
        "field": "pcr",
        "target_field": "pcr_pcr",
        "ignore_missing": true
      }
    },
    {
      "rename": {
        "field": "src",
        "target_field": "pcr_src",
        "ignore_missing": true
      }
    },
    {
      "set": {
        "field": "host.ip",
        "value": "{{pcr_src}}",
        "ignore_failure": true,
        "if": "ctx.pcr_src != null && ctx.host?.ip == null"
      }
    }
  ]
}
