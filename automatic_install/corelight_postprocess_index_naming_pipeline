{
  "description": "Corelight index naming pipeline. This pipeline is from the Github repository https://github.com/corelight/ecs-mapping. Please file all questions or issues with this configuration in the corresponding Github repository.",
  "processors": [

    {
      "set": {
        "description": "index prefix for - Protocol Logs (ie: non metrics,stats,system,etc) index prefix.",
        "field": "temporary_metadata_index_name_prefix",
        "value": "VAR_CORELIGHT_MAIN_INDEX_PREFIX",
        "if": "ctx.temporary_metadata_is_metric_log == 'no' && ctx.temporary_metadata_is_netcontrol_log == 'no' && ctx.temporary_metadata_is_stats_log == 'no' && ctx.temporary_metadata_is_system_log == 'no' && ctx.temporary_metadata_is_parse_failure == 'no'"
      }
    },
    {
      "set": {
        "description": "index prefix for - Non Protocols Logs (Metrics/Stats/System/Etc).",
        "field": "temporary_metadata_index_name_prefix",
        "value": "VAR_CORELIGHT_METRICS_INDEX_PREFIX",
        "if": "ctx.temporary_metadata_is_metric_log == 'yes' || ctx.temporary_metadata_is_netcontrol_log == 'yes' || ctx.temporary_metadata_is_stats_log == 'yes' || ctx.temporary_metadata_is_system_log == 'yes' && ctx.temporary_metadata_is_parse_failure == 'no'"
      }
    },
    {
      "set": {
        "description": "index prefix for - Protocol Logs but not parse %100 correctly but yet not field collision capable.",
        "field": "temporary_metadata_index_name_prefix",
        "value": "VAR_CORELIGHT_TEMPORARY_INDEX_PREFIX",
        "if": "ctx.temporary_metadata_index_name_suffix == null && ctx.temporary_metadata_is_parse_failure == 'no'"
      }
    },
    {
      "set": {
        "description": "index prefix for - Parse Failure Logs (parse-failures).",
        "field": "temporary_metadata_index_name_prefix",
        "value": "VAR_CORELIGHT_PARSE_FAILURES_INDEX_PREFIX",
        "if": "ctx.temporary_metadata_is_parse_failure == 'yes'"
      }
    },
    {
      "set": {
        "description": "index suffix for - Parse Failure Logs (parse-failures).",
        "field": "temporary_metadata_index_name_suffix",
        "value": "VAR_CORELIGHT_PARSE_FAILURES_INDEX_SUFFIX",
        "if": "ctx.temporary_metadata_is_parse_failure == 'yes'"
      }
    },
    {
      "set": {
        "description": "Set final _index name.",
        "field": "_index",
        "value": "{{temporary_metadata_index_name_prefix}}-{{temporary_metadata_index_name_suffix}}"
      }
    }
  ]
}




